# MedMemory 环境变量样例
# 复制为 .env.local 后填写实际值（.env.local 已被 .gitignore 排除）
#
# 所有 VITE_ 前缀变量会被 Vite 打进前端 bundle（纯前端 PWA 无法真正保密）
# 仅用于"开发/自用便利", 不应放生产敏感 key

# OpenAI API key（可选）
# - 留空: 用户在 Settings 页输入, 存 localStorage（推荐, 换设备时各自配置）
# - 填值: 作为初始默认, 用户在 Settings 页输入的 key 会覆盖此值
VITE_OPENAI_API_KEY=
