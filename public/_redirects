# Cloudflare Pages SPA fallback
#
# Vue Router history 模式下, /events/3 这种路径在服务端没有对应文件,
# 必须回退到 index.html 让前端 router 接管.
#
# 语法: <source> <destination> <status_code>
#   200 = rewrite (不是 redirect), 浏览器地址栏保持原 URL
#
# 排除静态资源: /assets/*, /_headers, /_redirects 等由 Cloudflare 直接处理,
# 不会命中 /* 规则 (Pages 内置优先级).
/*    /index.html   200
