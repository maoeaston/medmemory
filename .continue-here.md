# Current State

- **任务**：MedMemory 家庭医疗档案 PWA（自用）。**Schema bootstrap 自动 migration 已闭环**。
- **已完成**（按时间倒序）:
  - **Schema 自动 migration**（HEAD, 未 commit）:
    - `src/db/connection.ts`: 新增 `runMigrations(promiser)` + `SCHEMA_TARGET_VERSION=1`, 在 `getDb()` 首次初始化的 PRAGMA 之后调用——任何路径调 `getDb()`（生产 `createRepositories` / dev PoC）首次都会自动建表, 不再需要手动 `window.medmemoryPoc.runPoc()`
    - 幂等三重保险: schema_migrations 保底建表 + `SELECT version` 判断 + `001_initial.sql` 全量 `CREATE TABLE/INDEX IF NOT EXISTS` + `INSERT OR IGNORE INTO schema_migrations`
    - `db/migrations/001_initial.sql`: 8 张业务表 + schema_migrations 全改 `IF NOT EXISTS`, 末尾 `INSERT` 改 `INSERT OR IGNORE`
    - 验证: `npm run typecheck` ✓, `npm run build` ✓ (142 modules, 3.13s), dev server 起来 + `/src/db/connection.ts` Vite 已服务新代码
    - 未做浏览器端 E2E: 项目未入库 Playwright; 用户下次打开应用即可确认（无需先 runPoc）
  - **药箱 CRUD + 过期预警**（commit `29e6bec`）:
    - `MedicinesView.vue` 重写: filter tabs (全部/即将过期/已过期/家庭共用, badge 计数) + 名称/用途搜索 + 行内 status-{expired,soon,ok} 配色; Promise.allSettled 并行加载 medicines+members
    - `MedicineForm.vue` 新增: 参考 MemberForm; member_id 下拉; 编辑模式 member_id disabled (UpdateInput 类型排除 member_id)
    - 过期判定前端纯函数 statusOf, 与 Dashboard MedicineWarningPanel 对齐 (本月比对 + 30 天窗口)
  - **设置页 + 数据备份/恢复/重置**（commit `a703fa1`）
  - **事件 CRUD**（commit `a3b4066`）
  - **Inbox 多选归档**（commit `9c7b4a3`）
  - **Dashboard 完整 UI**（commit `471aca5`）
  - **成员管理 CRUD**（commit `56be907`）
  - **Quick Capture UI**（5 commits eaed8e4→9d82a7f）
  - **Repository 层 36/36 smoke test 通过**（commit `b1b9242`）
  - **基础设施 PoC** 通过: sqlite-wasm + OPFS + COOP/COEP
- **产出物**:
  - `src/composables/{useRepositories,useInboxArchive,useDataBackup}.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members,Events,EventDetailView,Medicines,Settings}View.vue` 完整; Timeline/Search 占位
  - `src/components/{capture,members,dashboard,inbox,events,medicines,ui}/`
  - `src/repositories/` 全套（8 表 + Search）

# Next Action（原子级）

**下一任务候选**, 按阻塞/优先级排序:

1. **时间线视图（推荐）**: PRD 7.7。按 event_date 聚合 medical_events（月分组 + 倒序）。入口 `/timeline`, 当前 `TimelineView.vue` 仅占位。高频入口可考虑 Dashboard 卡片直跳。
   - 第一步: `TimelineView.vue` 加 `useRepositories().medicalEvent.list()` 拉全量 events（list 已支持按 event_date DESC）→ 按月 `YYYY-MM` 分桶 → 渲染分组列表。先静态（无筛选），再按需加 member/event_type filter。
2. **健康问题关联**: PRD 7.4 MVP 已定 AI 推荐关联, 手工 CRUD 需重新评审
3. **Dashboard "上次备份 N 天前" 提醒**: Key Decisions 里已记; 配合 settings 的导出时间戳

**未提交提醒**: 当前 HEAD 有 schema migration 改动未 commit, 下次会话前建议 commit。

# Blockers

- 无（schema bootstrap 的冷启动 bug 已修）

# Key Decisions（≤ 3 条, 影响后续实现）

- **附件原件读取不缓存**: 每次mount 重新 IndexedDbStorageAdapter.getFile（家庭数据量小）
- **数据导出 zip 是 Flat + manifest.json**: `medmemory.sqlite3` + `blobs/{storage_key}` + manifest; 导入用 File System Access API 覆写 OPFS（sqlite-wasm open 不支持 byteArray 入参）
- **数据备份是长期安全底线**: feedback_self_use_ai_first.md; 后续建议加 Dashboard"上次备份 N 天前"提醒

# Relevant Files（最小集——下次任务起点）

- `src/views/TimelineView.vue` — 当前占位, 时间线视图实现起点
- `src/repositories/medicalEventRepository.ts` — `list()` 已支持按 event_date DESC
- `src/db/connection.ts:223-292` — `runMigrations()` 实现 + `getDb()` 集成（下次新增 002 migration 时参考）
- `db/migrations/001_initial.sql` — 所有 CREATE 已带 IF NOT EXISTS, INSERT 已带 OR IGNORE
- `src/db/poc.ts:82-84` — 注释提到"CREATE TABLE 无 IF NOT EXISTS" 已过时, 下次清理 PoC 时可一并更新
