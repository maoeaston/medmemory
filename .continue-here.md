# Current State

- **任务**：MedMemory 家庭医疗记忆系统（自用 PWA）。**Repository 层通过浏览器验证，进入 Domain + UI 层规划阶段**
- **已完成**：
  - v3.1 PRD 定稿（**仓库根** `家庭医疗记忆系统-v3.1.md`）
  - schema.sql：8 业务表 + schema_migrations
  - 4 篇 ADR（`docs/adr/ADR-001~004`）
  - 项目骨架：Vue3+TS+Vite，vite.config.ts 含 COOP/COEP
  - **基础设施层**（sqlite-wasm + OPFS）PoC 浏览器验证通过（2026-06-22）
    - `{tablesCreated:8, memberInserted:true, cascadeWorked:true, dbVersion:1, persistent:true}`
  - **Repository 实现层**（8 表 + SearchRepository）浏览器验证通过（2026-06-22）
    - `runRepositorySmokeTest()`: 36/36 step 全过, 0 失败, 2.1s
    - 覆盖 CRUD + 所有特有查询（range 过滤 / findOrCreate / attachBatch + JOIN / listExpiring 日期 / searchByKeyword + snippet + LEFT JOIN 未归档附件）
    - 12 个原子 commit: a1a2b7c → b1b9242
- **产出物**：
  - `src/repositories/` 完整目录（base.ts + 9 个实现 + index.ts + poc.ts）
  - `src/db/{connection,poc}.ts`
  - `src/storage/{StorageAdapter,IndexedDbStorageAdapter,keys}.ts`

# Next Action（原子级）

**进入 Domain + UI 层**。Repository 已就绪, 下一步按 PRD v3.1 优先级排序（MVP "Capture First" + 高频触点驱动长期留存）:

1. **路由 + 状态管理骨架**（vue-router + Pinia 已装）
   - 路由结构: Quick Capture / Dashboard / 时间线 / 药箱 / 搜索 / 成员管理
   - Pinia store 设计: 每个 feature 一个 store, 注入 `Repositories`
2. **Quick Capture UI**（PRD 7.1, MVP 最高优先级）
   - 入口: 全局快捷键 / Dashboard 顶部按钮 / PWA 安装后桌面图标直达
   - 三个 capture_type: photo（摄像头/上传）/ voice（录音）/ text（纯文字）
   - 数据流: UI → InboxRepository.create → StorageAdapter.saveFile（媒体）
3. **Dashboard UI**（PRD 7.2, 高频触点）
   - 成员卡片 + 过敏/慢病高亮 + 最近事件
   - "📥 N 条待整理" 入口（链向 Inbox 列表）
4. **Inbox 整理 UI**（PRD 7.1 归档路径）
   - inbox_items → 转成 medical_event + attachment
   - 媒体走 StorageAdapter → attachments.storage_key
5. **时间线 / 搜索 / 药箱** 按需迭代

每层通过后立即 commit, 保持分层铁律（下层未验证不写上层）。

# Blockers

- 无（Repository + 基础设施层均通过）

# Key Decisions（影响后续实现）

- **Repository 接口设计**（`interfaces.ts`）：每表一个 Repo；跨表搜索独立 `SearchRepository`；JSON 字段 TS 层强类型，实现层序列化；查询返回 null/[]，写入抛错；MVP 不分页
- **sqlite-wasm 走 `sqlite3Worker1Promiser` v2**（worker + OPFS）；不可用时降级 `:memory:`
- **Repository 删 metadata 行不删原件 Blob**：原件删除由 Domain 层显式调 `StorageAdapter.deleteFile(storage_key)`
- **Promiser v2 ESM 调用**：npm 包 `sqlite3Worker1Promiser` 导出本身就是 v2 工厂, 不需 `.v2()` 属性访问
- **Repository 实现层架构**（本次新增）：
  - base.ts 共享 helper（selectOne/selectMany/executeWrite/executeInsertReturningId/readJsonArray/writeJsonArray/fromDbBoolean/toDbBoolean/buildUpdateQuery）
  - 不做 abstract BaseRepository（接口差异大, generic 抽象会过度设计）
  - update 用白名单 buildUpdateQuery + 强制注入 updated_at（与 schema DEFAULT strftime 对齐）
  - create 用 `INSERT ... RETURNING id` 拿自增 PK（sqlite-wasm 3.53 支持）
  - 无事务: EventProblemRel.attachBatch 循环 exec + TODO 注释（家庭单用户 + problem 数量 <20 可接受）
  - JSON NULL → [] 兜底（parse 失败 console.warn, 不静默）
- **SearchRepository 用 LIKE 非 FTS**：MVP 足够; snippet 用 substr+instr 取关键词附近 200 字符; LEFT JOIN 处理未归档附件

# Relevant Files（最小集）

- `家庭医疗记忆系统-v3.1.md`（仓库根）— PRD 权威, 第 7 节方法集来源
- `db/migrations/001_initial.sql` — 数据模型权威
- `src/repositories/interfaces.ts` — Repository 接口契约
- `src/repositories/index.ts` — `createRepositories(): Promise<Repositories>` Domain 层入口
- `src/repositories/base.ts` — 共享 helper + RepositoryError
- `src/db/connection.ts` — `getDb(): Promise<DbHandle>` 基础契约
- `src/storage/StorageAdapter.ts` — Blob 存储（Quick Capture 媒体写入）
- `src/storage/keys.ts` — storage_key 生成规则
