# Current State

- **任务**：MedMemory 家庭医疗记忆系统。PRD 阶段已收尾（v3.1 定稿），进入系统设计/代码阶段，当前搭建基础设施层。
- **已完成**：
  - v3.1 PRD 定稿（`docs/家庭医疗记忆系统-v3.1.md`）
  - schema.sql：8 业务表 + schema_migrations，已用 python sqlite3 验证（外键 CASCADE、CHECK 约束、复合索引均生效）
  - 4 篇 ADR（`docs/adr/ADR-001~004`）
  - 项目骨架：Vue3+TS+Vite，vite.config.ts 含 COOP/COEP 头，sqlite-wasm 排除预打包
- **产出物**：`db/migrations/001_initial.sql`、`docs/adr/*`、`docs/家庭医疗记忆系统-v3.1.md`、`package.json`/`tsconfig.*`/`vite.config.ts`/`index.html`、`src/{main.ts,App.vue,vite-env.d.ts}`、`.gitignore`

# Next Action（原子级）

创建 `src/repositories/interfaces.ts`，定义 8 张表的 Repository TypeScript 接口。方法集合匹配 v3.1 PRD 第 7 节查询场景：Dashboard 最近一次就诊/体检/血常规、时间线（按时间/按健康问题）、关键词搜索（ocr_fulltext LIKE）、药箱过期预警（按 expiry_date）、按 storage_key 取附件、按 attachment 取最新 ai_contents。

# Blockers

- sqlite-wasm+OPFS PoC 需浏览器环境验证（WSL 无头环境无法验证 OPFS/COOP-COEP/SharedArrayBuffer），由用户在真实浏览器跑 `npm run dev`
- v1/v2 PRD 原文已不在项目目录（仅 v3/v3.1 保留）；v3.1 为权威版本，历史在其"文档版本"表有记录，不影响后续

# Key Decisions（影响后续实现）

- **持久化定 sqlite-wasm+OPFS，放弃 SQL.js**（ADR-001）；原件文件走 Storage Adapter 抽象（ADR-004），数据模型只持 storage_key
- **doc_type 大类枚举 + subtype 自由文本子类**；分类（doc_type/subtype）与标签（tags）是两种东西，不混用
- **health_problems 表结构保留但 MVP 不做手工 CRUD**，改为 AI 辅助关联 + 一键确认（避免录入超 2 分钟）
- 分层顺序：schema → Repository 接口 → sqlite-wasm PoC → Storage Adapter → 迁移机制 → Domain → UI。**不直接写 Vue 页面**

# Relevant Files（最小集）

- `db/migrations/001_initial.sql` — 数据模型权威
- `docs/家庭医疗记忆系统-v3.1.md` — PRD 权威（第 8 节数据模型 / 第 7 节功能查询场景）
- `docs/adr/ADR-001~004` — 架构决策
- `src/` — 待创建 `repositories/`、`storage/`、`db/`
