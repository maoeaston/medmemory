# Current State

- **任务**：MedMemory 家庭医疗档案 PWA（自用）。**OCR pipeline v1+v2 已闭环**。
- **已完成**（按时间倒序）:
  - **OCR pipeline v1+v2**（HEAD, 未 commit）:
    - **v2 schema**: `db/migrations/002_lab_indicators.sql` 新表 `report_indicators`（attachment_id FK CASCADE, name_cn/en, result, unit, reference_range, abnormal_tag CHECK H/L/N, display_order, model, prompt_version）; `connection.ts:runMigrations` 加 v2 分支
    - **v2 prompt**: `prompts.ts` PROMPT_VERSION='v2', 3 步结构（判型→输出 JSON→lab_indicators 填充规则）; abnormal_tag 判定规则（H/L/N/null）写死在 prompt
    - **v2 provider**: `OpenAiProvider.parseMedicalDocumentJson` 校验 doc_type 枚举 + report_type/hospital_name/test_date 类型 + lab_indicators 数组（非 lab_report 拒绝非空指标, 单条非法 abnormal_tag 降级 null 不整批 FAILED）
    - **v2 repository**: `ReportIndicatorRepositoryImpl.{createBatch,listByAttachment,deleteByAttachment}` 整批语义, 无单条 create
    - **v2 composable**: `useAiProcess.writeResults` 落盘 4 路: ai_contents(summary+ocr) 并行 → updateDocType（subtype=reportType）→ 若 labIndicators 非空: deleteByAttachment+createBatch（batch replace）→ updateTags
    - **v2 UI**: `AttachmentPreview.vue` 加 doc_type 下拉（手动兜底, subtype 保留）+ 化验单指标表（H=红/L=蓝/N=灰 abnormal-tag 高亮）; 每次 toggleExpand 强制 reload（重新生成场景刷新缓存）
    - **v1 基础**: `AiProvider` 抽象 + `OpenAiProvider` (GPT-4o Vision, response_format json_object, FileReader.readAsDataURL); `useAiConfig` localStorage+env 单例; `useAiProcess.{processAttachment,processBatch,isApiKeyError}` 状态机编排 UPLOADED→OCR_PROCESSING→SUMMARY_DONE / FAILED; `useInboxArchive` archive 后 fire-and-forget dynamic import 触发 AI
    - **v1 UI**: `SettingsView.vue` AI 配置区（API key password 输入 + 保存）+ 批量处理区（listPendingAi + 进度）; `SearchView.vue` 从 placeholder 重写（300ms debounce + member filter, 消费 ocr_fulltext）; `AttachmentPreview.vue` 4 状态机 + 3s 轮询同步后台处理
    - 验证: `npm run typecheck` ✓, `npm run build` ✓ (3.80s, useAiProcess chunk 8.93kB); 未做浏览器 E2E（需用户自备 OpenAI API key）
  - **时间线 member filter**（commit `bf730ba`）
  - **时间线视图 `/timeline`**（commit `f252980`）
  - **Schema 自动 migration bootstrap**（commit `a0dda69`）
  - **药箱 CRUD + 过期预警**（commit `29e6bec`）
  - **设置页 + 数据备份/恢复/重置**（commit `a703fa1`）
  - **事件 CRUD**（commit `a3b4066`）
  - **Inbox 多选归档**（commit `9c7b4a3`）
  - **Dashboard 完整 UI**（commit `471aca5`）
  - **成员管理 CRUD**（commit `56be907`）
  - **Quick Capture UI**（5 commits eaed8e4→9d82a7f）
  - **Repository 层 36/36 smoke test 通过**（commit `b1b9242`）
  - **基础设施 PoC** 通过: sqlite-wasm + OPFS + COOP/COEP
- **产出物**:
  - `src/lib/ai/{AiProvider,OpenAiProvider,prompts}.ts` 全套 provider
  - `src/composables/{useRepositories,useInboxArchive,useDataBackup,useAiConfig,useAiProcess}.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members,Events,EventDetailView,Medicines,Settings,Timeline,Search}View.vue` 完整
  - `src/components/{capture,members,dashboard,inbox,events,medicines,ui}/`
  - `src/repositories/` 全套（9 表 + Search, 含 ReportIndicator）
  - `db/migrations/001_initial.sql` + `002_lab_indicators.sql`

# Next Action（原子级）

**下一任务候选**, 按阻塞/优先级排序:

1. **浏览器端 E2E 验证（推荐, 解锁 confidence）**:
   需用户自备 OpenAI API key. 流程: Settings 填 key → Capture 拍一张医疗单据（化验单/处方/门诊记录各一张）→ Inbox 归档为某成员事件 → 进事件详情看 AttachmentPreview 状态跃迁 UPLOADED→OCR_PROCESSING→SUMMARY_DONE → 展开看 summary + 指标表（化验单）+ OCR 全文 → 进 /search 搜关键词命中 → 故障路径（错 key）看 FAILED + processing_error. 批量处理: 留 2-3 个未处理附件, Settings 批量处理入口跑完.
2. **Dashboard "上次备份 N 天前" 提醒**:
   `useDataBackup.exportAll()` 成功时 `localStorage['medmemory:lastBackupAt']=new Date().toISOString()`; DashboardView 加卡片: 无记录或 >7 天 → 黄色高亮 + "立即备份" 按钮; import 视为新基准, reset 清空. 仅改 `useDataBackup.ts` + `DashboardView.vue` 两文件.
3. **健康问题关联**: PRD 7.4 MVP 已定 AI 推荐关联, 手工 CRUD 需重新评审

# Blockers

- None（浏览器 E2E 需用户提供 API key, 但不阻塞代码继续推进）

# Key Decisions（≤ 3 条, 影响后续实现）

- **OCR pipeline v2 决策锁定**: 化验单专属结构化提取（report_indicators 新表, 非 JSON 字段）; LLM 自动判 doc_type + UI 手动兜底; abnormal_tag 完全信任 LLM 不做本地校验, UI 只做颜色高亮（H=红/L=蓝/N=灰）
- **test_date / hospital_name 暂不持久化**: schema 无对应列, 已在 summary/ocr_fulltext 内可检索; 若后续 UI 需独立展示再加 schema 列, 避免 migration churn
- **lab_indicators 整批替换语义（非多版本）**: 重新生成 = DELETE WHERE attachment_id + INSERT 新批. 多版本语义在 ai_contents.summary/ocr_fulltext 已有, 指标版本管理复杂度收益低

# Relevant Files（最小集——浏览器 E2E + 后续 maintenance）

- `src/composables/useAiProcess.ts` — AI 处理编排核心, writeResults 4 路落盘
- `src/lib/ai/{OpenAiProvider.ts,prompts.ts}` — provider 实现 + prompt v2
- `src/components/events/AttachmentPreview.vue` — 4 状态机 + doc_type 下拉 + 指标表
- `db/migrations/002_lab_indicators.sql` — v2 schema
- `.env.example` — `VITE_OPENAI_API_KEY` 配置说明
