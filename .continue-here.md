# Current State

- **任务**：MedMemory 家庭医疗档案 PWA（自用）。**药箱 CRUD + 过期预警已闭环**。
- **已完成**（按时间倒序）:
  - **药箱 CRUD + 过期预警**（本轮, 待 commit）:
    - `MedicinesView.vue` 重写: filter tabs (全部 / 即将过期 / 已过期 / 家庭共用, 带 badge 计数) + 按名称/用途搜索 + 单列表 + 行内 status-{expired,soon,ok} 配色; 并行加载 medicines + members (Promise.allSettled, members 失败不阻塞主列表)
    - `MedicineForm.vue` 新增: 参考 MemberForm 结构; member_id 下拉 (家庭共用 + members); 编辑模式 member_id disabled + 提示「创建后不可更改」（UpdateInput 类型排除 member_id）
    - 过期判定前端纯函数 (statusOf), 与 Dashboard MedicineWarningPanel 对齐 (本月比对 + 30 天窗口); 不调 listExpiring 省 DB 查询
    - E2E 14/14 PASS (Playwright): 空态 / 创建 / 必填校验 / 编辑预填 + member disabled / 删除 ConfirmDialog / filter tabs / 搜索 全过
    - **发现既有 bug（未修, 见 Next Action #1）**: 应用启动未自动跑 schema migrations, 新 OPFS 冷启动 `no such table`。E2E 里临时调 `window.medmemoryPoc.runPoc()` 绕过
  - **设置页 + 数据备份/恢复/重置**（commit `a703fa1`）:
    - `useDataBackup.ts`: `exportAllData` / `importAllData` / `resetAllData` 编排, 进度回调
    - `connection.ts`: `PromiserFn` 加 `'export'`, `DbHandle.exportBytes()`, `closeDb({unlink})` 类型扩展
    - `SettingsView.vue` 重写: 3 section + 导出自动下载 + 导入 ConfirmDialog + 重置两道确认（第二道输入"我确认删除"）
    - zip 结构: `medmemory.sqlite3` + `manifest.json` + `blobs/{storage_key}`, flat
    - 关键修正: `closeDb({unlink:true})` 对 opfs VFS 不可靠, 改用 `navigator.storage.getDirectory().removeEntry()` 显式删文件
    - E2E 全链路通过: 导出/导入覆盖/重置 cancel+正确路径
  - **事件 CRUD**（commit `a3b4066`）: EventsView 列表 + EventDetailView + EventEditForm modal + AttachmentPreview + 404
  - **Inbox 多选归档**（commit `9c7b4a3`）
  - **Dashboard 完整 UI**（commit `471aca5`）
  - **成员管理 CRUD**（commit `56be907`）
  - **Quick Capture UI**（5 commits eaed8e4→9d82a7f）
  - **Repository 层 36/36 smoke test 通过**（commit `b1b9242`）
  - **基础设施 PoC** 通过: sqlite-wasm + OPFS + COOP/COEP
- **产出物**:
  - `src/composables/{useRepositories,useInboxArchive,useDataBackup}.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members,Events,EventDetailView,Medicines,Settings}View.vue` 完整; Timeline/Search 占位
  - `src/components/{capture,members,dashboard,inbox,events,medicines,ui}/`
  - `src/repositories/` 全套（8 表 + Search）

# Next Action（原子级）

**下一任务候选**, 按阻塞/优先级排序:

1. **Schema bootstrap 自动迁移（推荐, 阻塞所有新用户/新设备冷启动）**: 当前 `getDb()` 只 PRAGMA, 不建表; 建表靠 `window.medmemoryPoc.runPoc()` 手动跑。需在 `getDb()` 初始化末尾加 migration 逻辑: 查 `schema_migrations`, 缺哪些 version 跑哪些 SQL。最小侵入: `001_initial.sql` 的 `CREATE TABLE` 改 `CREATE TABLE IF NOT EXISTS` 保证幂等。工作量: 小（约 50 行 + sql 微调）
2. **时间线视图**: PRD 7.7。按 event_date 聚合 medical_events 成病程时间轴。只读聚合页。入口 `/timeline`, 当前占位。工作量: 最小
3. **健康问题关联**: PRD 7.4 MVP 已定"AI 推荐关联"而非手工 CRUD。若要做需重新评审

**第一步（schema bootstrap 路径, 用户确认后）**: 在 `src/db/connection.ts` 的 `getDb()` PRAGMA 之后加: (a) `CREATE TABLE IF NOT EXISTS schema_migrations (...)` 保底, (b) `SELECT version FROM schema_migrations`, (c) 缺 version 1 时执行 `getSchemaSql()`, (d) `INSERT INTO schema_migrations (version) VALUES (1)`。同步把 `db/migrations/001_initial.sql` 里 8 张业务表的 `CREATE TABLE` 改为 `CREATE TABLE IF NOT EXISTS`（去掉 sqlite_% 系统表那部分）。

# Blockers

- **既有 bug（不阻塞本任务, 阻塞新用户冷启动）**: 应用启动未自动跑 schema migrations。临时 workaround: console 跑 `window.medmemoryPoc.runPoc()`

# Key Decisions（影响后续实现, ≤ 3 条）

- **附件原件读取走 IndexedDbStorageAdapter.getFile(storage_key)**: 详情页/卡片每次 mount 重新加载, 不缓存（家庭数据量小, 简单）
- **数据导出 zip 是 Flat + manifest.json**: `medmemory.sqlite3` + `blobs/{storage_key}` + manifest; 导入用 File System Access API 覆写 OPFS（sqlite-wasm open 不支持 byteArray 入参）
- **重置/导入成功后 location.reload()**: 绕过组件 ref 残留; resetAllData 不调 reload（由 UI 决定）
- **数据备份是长期安全底线**: 用户多次强调（feedback_self_use_ai_first.md）。后续建议加 Dashboard "上次备份 N 天前" 提醒

# Relevant Files（最小集——schema bootstrap 路径所需）

- `src/db/connection.ts:249-315` — `getDb()` 单例 + PRAGMA, 需在此加 migration 调用
- `src/db/connection.ts:351-353` — `getSchemaSql()` 已有, 返回 001_initial.sql 字符串
- `db/migrations/001_initial.sql` — 8 张业务表 + schema_migrations; CREATE TABLE 需改 IF NOT EXISTS
- `src/db/poc.ts:85-110` — runPoc 已实现"执行 schema + 验证 schema_migrations.version" 模式, 可参考
- `src/views/MedicinesView.vue` / `src/components/medicines/MedicineForm.vue` — 本轮新增, 已闭环
