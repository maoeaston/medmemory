# Current State

- **任务**：MedMemory 家庭医疗档案 PWA（自用）。**时间线视图 + member filter 已闭环**。
- **已完成**（按时间倒序）:
  - **时间线 member filter**（HEAD, 未 commit）:
    - `TimelineView.vue`: 加 `selectedMemberId` ref（null=全部成员）+ `filteredEvents` computed（前端 filter, YAGNI 不走 DB）; header 下 `<select>` 下拉（"全部成员" + members, `v-model.number` + `:value` 绑定保类型）; filter 生效时隐藏行内成员名（去重噪声, 医院/科室保留）; 区分两种空态（"无任何事件" vs "该成员无事件, 切换全部成员"）; count badge 跟 filteredEvents.length
    - 验证: `npm run typecheck` ✓, `npm run build` ✓ (4.17s); 未做浏览器端 E2E（v-model.number + null option 类型转换已静态分析验证）
  - **时间线视图 `/timeline`**（commit `f252980`）:
    - `MedicalEventRepository.listAll()`: 新增方法, 跨成员按 `event_date DESC, id DESC` 查询（区别于 `listRecent` 的 `created_at DESC` "最近录入"语义）
    - `TimelineView.vue` 重写: `listAll() + familyMember.list()` 并行 → 按 `event_date.slice(0,7)` YYYY-MM 分桶 → 月标题 sticky + 组内事件倒序; 行交互同 EventsView（点击跳 `/events/:id`）; 格式化 `YYYY 年 M 月` / `M-D`
    - 验证: `npm run typecheck` ✓, `npm run build` ✓ (2.93s)
    - 未做浏览器端 E2E: 项目未入库 Playwright
  - **Schema 自动 migration**（commit `a0dda69`）: `getDb()` 首次初始化自动建表 + 三重幂等保险（schema_migrations 保底 + SELECT version 判断 + SQL 全 IF NOT EXISTS / INSERT OR IGNORE）, 老路径 `runPoc()` 不再必需
  - **药箱 CRUD + 过期预警**（commit `29e6bec`）
  - **设置页 + 数据备份/恢复/重置**（commit `a703fa1`）
  - **事件 CRUD**（commit `a3b4066`）
  - **Inbox 多选归档**（commit `9c7b4a3`）
  - **Dashboard 完整 UI**（commit `471aca5`）
  - **成员管理 CRUD**（commit `56be907`）
  - **Quick Capture UI**（5 commits eaed8e4→9d82a7f）
  - **Repository 层 36/36 smoke test 通过**（commit `b1b9242`）
  - **基础设施 PoC** 通过: sqlite-wasm + OPFS + COOP/COEP
- **产出物**:
  - `src/composables/{useRepositories,useInboxArchive,useDataBackup}.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members,Events,EventDetailView,Medicines,Settings,Timeline}View.vue` 完整; Search 占位
  - `src/components/{capture,members,dashboard,inbox,events,medicines,ui}/`
  - `src/repositories/` 全套（8 表 + Search）

# Next Action（原子级）

**下一任务候选**, 按阻塞/优先级排序:

1. **Dashboard "上次备份 N 天前" 提醒（推荐, 直扣"数据备份是长期安全底线"决策）**:
   `useDataBackup.exportAll()` 成功时 `localStorage['medmemory:lastBackupAt']=new Date().toISOString()`;
   DashboardView 加卡片: 无记录或 >7 天 → 黄色高亮 + "立即备份" 按钮（跳 /settings）; 否则灰色显示"N 天前备份". 仅改 `useDataBackup.ts` + `DashboardView.vue` 两文件.
2. **健康问题关联**: PRD 7.4 MVP 已定 AI 推荐关联, 手工 CRUD 需重新评审
3. **Search 落地**: 当前 SearchView 占位, 需 sqlite FTS5 或前端 substring

**第一步（Dashboard 备份提醒, 用户确认后）**:
`src/composables/useDataBackup.ts:exportAll()` 成功路径末尾加 `localStorage.setItem('medmemory:lastBackupAt', new Date().toISOString())`;
`DashboardView.vue` 加备份状态 computed（读 localStorage + 算天数差）+ 卡片 UI（同 MedicineWarningPanel 风格）;
别忘了 `importBackup`/`resetAll` 路径也要更新 lastBackupAt（import 可视为新基准, reset 清空）.

**未提交提醒**: 当前 HEAD 有时间线 member filter + .continue-here.md 改动未 commit, 本会话将 commit.

# Blockers

- None

# Key Decisions（≤ 3 条, 影响后续实现）

- **EventsView vs TimelineView 职责分开**: EventsView = `listRecent` 按 `created_at` "最近录入"; TimelineView = `listAll` 按 `event_date` "按发生时间回溯" + 月分组. 两个入口语义不重叠, 不合并.
- **Timeline filter 走前端 computed 不走 DB**: listAll() 已拉全量, 单页数据量小, 前端 filter 即可. YAGNI 不给 `listAll()` 加 member_id 参数（feedback_long_term_data_architecture: schema 长期优先, 但 query 参数 YAGNI）
- **数据备份是长期安全底线**: feedback_self_use_ai_first.md; 下一候选 Dashboard 备份提醒直接对应此约束

# Relevant Files（最小集——Dashboard 备份提醒路径所需）

- `src/composables/useDataBackup.ts` — export 成功时写 `localStorage['medmemory:lastBackupAt']`; import/reset 路径也要更新
- `src/views/DashboardView.vue` — 加备份状态卡片（参考 MedicineWarningPanel 风格）
- `src/components/dashboard/MedicineWarningPanel.vue` — 卡片样式参考
- `.continue-here.md` 旧 handoff（member filter 路径）已在 git history 里
