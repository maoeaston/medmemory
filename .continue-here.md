# Current State

- **任务**：MedMemory 家庭医疗档案 PWA（自用）。**药箱 CRUD 已闭环**。
- **已完成**（按时间倒序）:
  - **药箱 CRUD + 过期预警**（commit `29e6bec`）:
    - `MedicinesView.vue` 重写: filter tabs (全部/即将过期/已过期/家庭共用, badge 计数) + 名称/用途搜索 + 行内 status-{expired,soon,ok} 配色; Promise.allSettled 并行加载 medicines+members
    - `MedicineForm.vue` 新增: 参考 MemberForm; member_id 下拉; 编辑模式 member_id disabled (UpdateInput 类型排除 member_id)
    - 过期判定前端纯函数 statusOf, 与 Dashboard MedicineWarningPanel 对齐 (本月比对 + 30 天窗口)
    - E2E 14/14 PASS (Playwright)
    - **发现既有 bug 未修（见 Next Action #1）**: 应用启动未自动跑 schema migrations
  - **设置页 + 数据备份/恢复/重置**（commit `a703fa1`）
  - **事件 CRUD**（commit `a3b4066`）
  - **Inbox 多选归档**（commit `9c7b4a3`）
  - **Dashboard 完整 UI**（commit `471aca5`）
  - **成员管理 CRUD**（commit `56be907`）
  - **Quick Capture UI**（5 commits eaed8e4→9d82a7f）
  - **Repository 层 36/36 smoke test 通过**（commit `b1b9242`）
  - **基础设施 PoC** 通过: sqlite-wasm + OPFS + COOP/COEP
- **产出物**:
  - `src/composables/{useRepositories,useInboxArchive,useDataBackup}.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members,Events,EventDetailView,Medicines,Settings}View.vue` 完整; Timeline/Search 占位
  - `src/components/{capture,members,dashboard,inbox,events,medicines,ui}/`
  - `src/repositories/` 全套（8 表 + Search）

# Next Action（原子级）

**下一任务候选**, 按阻塞/优先级排序:

1. **Schema bootstrap 自动迁移（推荐, 阻塞新用户冷启动）**: 当前 `getDb()` 只 PRAGMA, 不建表; 建表靠 `window.medmemoryPoc.runPoc()` 手动跑
2. **时间线视图**: PRD 7.7。按 event_date 聚合 medical_events。入口 `/timeline`, 当前占位
3. **健康问题关联**: PRD 7.4 MVP 已定 AI 推荐关联, 手工 CRUD 需重新评审

**第一步（schema bootstrap 路径, 用户确认后）**: 在 `src/db/connection.ts:269-279` 的 PRAGMA 之后插入: (a) `CREATE TABLE IF NOT EXISTS schema_migrations (version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ','now')))` 保底; (b) `SELECT version FROM schema_migrations`; (c) 缺 version 1 时 `db.exec(getSchemaSql())`; (d) `INSERT INTO schema_migrations (version) VALUES (1)`。同步把 `db/migrations/001_initial.sql` 的 8 张业务表 `CREATE TABLE` 改 `CREATE TABLE IF NOT EXISTS`（schema_migrations 已有 PRIMARY KEY, IF NOT EXISTS 幂等）。

# Blockers

- **既有 bug（不阻塞本任务, 阻塞新用户冷启动）**: 应用启动未自动跑 schema migrations。Workaround: console 跑 `window.medmemoryPoc.runPoc()`

# Key Decisions（≤ 3 条, 影响后续实现）

- **附件原件读取不缓存**: 每次mount 重新 IndexedDbStorageAdapter.getFile（家庭数据量小）
- **数据导出 zip 是 Flat + manifest.json**: `medmemory.sqlite3` + `blobs/{storage_key}` + manifest; 导入用 File System Access API 覆写 OPFS（sqlite-wasm open 不支持 byteArray 入参）
- **数据备份是长期安全底线**: feedback_self_use_ai_first.md; 后续建议加 Dashboard"上次备份 N 天前"提醒

# Relevant Files（最小集——schema bootstrap 路径所需）

- `src/db/connection.ts:249-315` — `getDb()` 单例 + PRAGMA, 需在此加 migration
- `src/db/connection.ts:351-353` — `getSchemaSql()` 已有
- `db/migrations/001_initial.sql` — 8 张业务表 + schema_migrations; CREATE TABLE 需改 IF NOT EXISTS
- `src/db/poc.ts:85-110` — runPoc 已实现"执行 schema + 验证 version" 模式, 可参考
