# Current State

- **任务**：MedMemory 家庭医疗档案 PWA（自用）。**事件 CRUD + 设置页（导出/导入/重置）已闭环**。
- **已完成**（按时间倒序）:
  - **设置页 + 数据备份/恢复/重置**（本轮, 待 commit）:
    - `useDataBackup.ts`: `exportAllData` / `importAllData` / `resetAllData` 编排, 进度回调
    - `connection.ts`: `PromiserFn` 加 `'export'`, `DbHandle.exportBytes()`, `closeDb({unlink})` 类型扩展
    - `SettingsView.vue` 重写: 3 section + 导出自动下载 + 导入 ConfirmDialog + 重置两道确认（第二道输入"我确认删除"）
    - zip 结构: `medmemory.sqlite3` + `manifest.json` + `blobs/{storage_key}`, flat
    - 关键修正: `closeDb({unlink:true})` 对 opfs VFS 不可靠, 改用 `navigator.storage.getDirectory().removeEntry()` 显式删文件
    - E2E 全链路通过: 导出/导入覆盖/重置 cancel+正确路径
  - **事件 CRUD**（commit `a3b4066`）: EventsView 列表 + EventDetailView + EventEditForm modal + AttachmentPreview + 404
  - **Inbox 多选归档**（commit `9c7b4a3`）
  - **Dashboard 完整 UI**（commit `471aca5`）
  - **成员管理 CRUD**（commit `56be907`）
  - **Quick Capture UI**（5 commits eaed8e4→9d82a7f）
  - **Repository 层 36/36 smoke test 通过**（commit `b1b9242`）
  - **基础设施 PoC** 通过: sqlite-wasm + OPFS + COOP/COEP
- **产出物**:
  - `src/composables/{useRepositories,useInboxArchive,useDataBackup}.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members,Events,EventDetailView,Settings}View.vue` 完整; Timeline/Search/Medicines 占位
  - `src/components/{capture,members,dashboard,inbox,events,ui}/`
  - `src/repositories/` 全套（8 表 + Search）

# Next Action（原子级）

**下一任务待用户决定**, 候选（按用户偏好"高频触点驱动长期留存"排序）:

1. **药箱 CRUD + 过期预警**（推荐）: PRD 7.10。高频日常（用药检查/过期）。Dashboard 已有只读预警面板。工作量: 列表/CRUD/筛选 UI。入口 `/medicines`, 当前占位
2. **时间线视图**: PRD 7.7。按 event_date 聚合事件。入口 `/timeline`, 当前占位
3. **健康问题关联**: PRD 7.4 手工 CRUD（AI 辅助暂缓）+ event_problem_rel 多对多挂接。工作量大

**第一步（药箱路径, 用户确认后）**: 把 `src/views/MedicinesView.vue` 从占位改为列表页, 用 `medicine.listAll()` + `medicine.listExpiring(30)` 分两段渲染, 行点击打开 MedicineForm modal（参考 MembersView 模式）。

# Blockers

- None

# Key Decisions（影响后续实现, ≤ 3 条）

- **附件原件读取走 IndexedDbStorageAdapter.getFile(storage_key)**: 详情页/卡片每次 mount 重新加载, 不缓存（家庭数据量小, 简单）
- **数据导出 zip 是 Flat + manifest.json**: `medmemory.sqlite3` + `blobs/{storage_key}` + manifest; 导入用 File System Access API 覆写 OPFS（sqlite-wasm open 不支持 byteArray 入参）
- **重置/导入成功后 location.reload()**: 绕过组件 ref 残留; resetAllData 不调 reload（由 UI 决定）
- **数据备份是长期安全底线**: 用户多次强调（feedback_self_use_ai_first.md）。后续建议加 Dashboard "上次备份 N 天前" 提醒

# Relevant Files（最小集——药箱路径所需）

- `src/views/MedicinesView.vue` — 列表页（当前占位）
- `src/repositories/interfaces.ts:369-390` — MedicineRepository（listAll/listByMember/listExpiring/listByUsage）
- `src/views/MembersView.vue` — modal CRUD UI 模式参考
- `src/components/members/MemberForm.vue` — 表单结构参考
- `src/components/ui/{ModalOverlay,ConfirmDialog}.vue` — 复用
- `src/composables/useRepositories.ts` — Repository 入口
- `db/migrations/001_initial.sql:168-186` — medicines schema（字段约束）
