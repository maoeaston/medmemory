# Current State

- **任务**: MedMemory 家庭医疗档案 PWA（自用）。**OCR pipeline v3 + 事件级 summary 回写 + AI 健康问题推荐闭环（代码层）**。
- **已完成**（按时间倒序）:
  - **7.4 AI 辅助健康问题关联 (v3.1 核心重构)**（HEAD, 未 commit）:
    - migration 003: ai_contents.content_type CHECK 加 'suggested_health_problems', SQLite 表重建流程
    - prompts v2→v3: MEDICAL_DOCUMENT_PROMPT 加 suggested_health_problems 字段 + 4 步规则
    - AiProcessingResult.SuggestedHealthProblem 类型 + OpenAiProvider 校验 (非法元素降级丢弃, 不 FAILED 整批)
    - writeResults 第 6 路: 每个 suggestion 一条 ai_contents (content_type='suggested_health_problems')
    - AiContentRepository.listPendingSuggestionsByEvent (JOIN attachments)
    - EventDetailView 健康问题区块: 已关联 tags (detach) + 手动添加 + AI 待确认列表 (确认/跳过)
    - typecheck ✓, build ✓ (2.81s)
  - **事件级 summary 回写闭环**（HEAD, 未 commit）: `useAiProcess.ts:writeResults` 加第 5 路 — attachment.event_id 非空时读 event, `!event.summary` 时写 `result.summary`。首个胜出 + 保护用户手动编辑。**UI 侧 TimelineView.event-summary (commit f252980) + EventDetailView.summary-section (commit a3b4066) 早就存在**, 本次回写打通数据链路后自动生效。typecheck ✓, build ✓ (3.81s)
  - **OpenAI 兼容 Base URL 配置**（HEAD, 未 commit）:
    - `useAiConfig.ts`: 加 `baseUrl` + `model` refs + `saveBaseUrl`/`saveModel`, localStorage key 改为 `medmemory:aiApiKey`/`aiBaseUrl`/`aiModel`, 无默认值魔法（用户必填）
    - `OpenAiProvider.ts`: 构造函数接 `baseUrl` 参数, 拼 `${baseUrl}/chat/completions`, **移除 `endsWith('/chat/completions')` 容错**（严格 SDK 行为, 符合 ccapi.us 主流约定）
    - `useAiProcess.ts`: 修 critical bug — `callProvider` 把 baseUrl/model 传给 provider; 入口加 baseUrl/model 非空校验; `writeResults` 用真实 model 名写入 ai_contents/report_indicators
    - `SettingsView.vue`: AI 配置区从 1 栏（API key）扩展到 3 栏（API Key + Base URL + Model）, 各自独立保存
    - 验证: typecheck ✓, build ✓ (3.74s)
  - **OCR pipeline v1+v2**（commit `1330130`）: AiProvider/OpenAiProvider/prompts v2 + useAiConfig/useAiProcess + migration 002 report_indicators + ReportIndicatorRepository + AttachmentPreview 4 状态机 + doc_type 下拉 + 化验单指标表（H/L/N 高亮）+ SettingsView 批量处理 + SearchView 重写 + useInboxArchive fire-and-forget
  - **时间线 member filter**（commit `bf730ba`）
  - **时间线视图 `/timeline`**（commit `f252980`）
  - **Schema 自动 migration**（commit `a0dda69`）
  - **药箱 CRUD + 过期预警**（commit `29e6bec`）
  - **设置页 + 数据备份**（commit `a703fa1`）
  - **事件 CRUD / Inbox 归档 / Dashboard / 成员管理 / Quick Capture**（5+ commits）
- **产出物**:
  - `src/lib/ai/{AiProvider,OpenAiProvider,prompts}.ts` 全套 provider
  - `src/composables/{useRepositories,useInboxArchive,useDataBackup,useAiConfig,useAiProcess}.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members,Events,EventDetailView,Medicines,Settings,Timeline,Search}View.vue` 完整
  - `src/repositories/` 全套（9 表 + Search + ReportIndicator）
  - `db/migrations/{001_initial,002_lab_indicators}.sql`

# Next Action（原子级）

**AI 健康问题关联闭环已完成 (代码层), 待提交 + E2E 验证**。

剩余动作:
1. **提交**: HEAD 有多个文件改动 (useAiProcess / prompts / AiProvider / OpenAiProvider / interfaces / aiContentRepository / EventDetailView / connection.ts / migration 003 / .continue-here.md)
   - 建议 commit message: `feat(7.4): AI 辅助健康问题关联 (v3.1 核心重构)`
   - 或拆两个 commit: ① `feat(ai): 事件级 summary 回写` ② `feat(7.4): AI 健康问题关联 (prompt v3 + migration 003 + UI)`
2. **浏览器 E2E** (关键, 因为本次改动横跨 7 个文件 + migration + prompt 升级):
   - 先验证 migration 003 自动执行（全新库 / 已有 v2 数据库两种路径）
   - SettingsView 配 API key/baseUrl/model
   - 上传化验单 → AI 处理 → 进 EventDetailView 应看到 "AI 推荐关联" 区块
   - 点"确认" → 问题进入已关联 tags, 推荐消失
   - 点"跳过" → 推荐消失, 不关联
   - 手动输入回车 → 加入已关联
   - 点已关联 × → 移除
   - 数据库验证: SELECT * FROM health_problems / event_problem_rel / ai_contents WHERE content_type='suggested_health_problems'
3. **后续可选** (等实际使用反馈再判):
   - prompt 调优 (v3 文案先跑通, 实际推荐不准再迭代)
   - 时间线视图 B (按健康问题聚合事件, 依赖 health_problems 表有数据后才有意义)
   - 纯文本事件 AI 推荐 (当前仅附件触发, 无附件用"手动关联"入口)
   - EventDetailView 加 "AI 重新推荐" 按钮 (当前推荐被 ignore 后无法恢复)

# Blockers

- None（浏览器 E2E 未做, 但不阻塞代码推进）

# Key Decisions（≤ 3 条, 影响后续实现）

- **事件 summary 首个胜出策略**: 多附件事件, 第一个完成 AI 处理的附件 summary 写入 event.summary, 后续不覆盖。避免反复覆盖 + 保护用户手动编辑。如果用户想刷新, 需要单独 UI 入口（暂不做）
- **OpenAI 兼容 Base URL**: 用户配置 baseUrl/model, 代码不写死。支持 ccapi.us 等中转站切换（见 PROJECT_CONTEXT.md "AI Provider 落地"）
- **endpoint 拼接严格 SDK 行为**: 用户填到 `/v1`, 代码必然拼 `/chat/completions`。不做"用户已拼全则保留"容错（违背主流约定）

# Relevant Files（最小集——事件级 summary 闭环）

- `src/composables/useAiProcess.ts` — writeResults 第 5 路（已改）
- `src/views/TimelineView.vue` — 事件卡片 summary（早已实现, 无需改）
- `src/views/EventDetailView.vue` — 事件详情 summary 区块（早已实现, 无需改）
