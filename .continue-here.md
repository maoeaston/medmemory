# Current State

- **任务**：MedMemory 家庭医疗记忆系统 PWA（自用）。**Inbox 多选归档流程已闭环**（photo 迁移到 attachment, 语音/文字 archive only）
- **已完成**（按时间倒序）:
  - **Inbox 多选归档**（commit `9c7b4a3`, 2026-06-22）:
    - `useInboxArchive.ts` 编排函数: medicalEvent.create → 逐条 photo 迁移 Blob + create attachment → inbox.archive; 单条失败不中断; attachment.create 失败回滚新 Blob
    - `InboxItemCard.vue` / `ArchiveForm.vue` / `InboxView.vue` 完整闭环
    - 浏览器验证: 8 条混合类型归档为 event #8, 3 张 photo 正确转 attachment (`attachment/7/...png` 格式)
  - **Dashboard 完整 UI**（commit `471aca5`）: DashboardHeader/MemberCard/MedicineWarningPanel/RecentEventsPanel, 过敏红底高亮, Promise.allSettled 并行加载
  - **成员管理 CRUD**（commit `56be907`）: ModalOverlay/ConfirmDialog/StringListEditor/AllergyEditor/ChronicConditionEditor/MemberForm/MembersView
  - **Quick Capture UI**（5 commits eaed8e4→9d82a7f）: 3 capture 组件 + 路由 + KeepAlive
  - **Repository 层 36/36 smoke test 通过**（commit b1b9242）
  - **基础设施 PoC** 通过: sqlite-wasm + OPFS + COOP/COEP
- **产出物**:
  - `src/composables/useRepositories.ts` / `useInboxArchive.ts`
  - `src/views/{Dashboard,Capture,Inbox,Members}View.vue` 完整; 其余占位
  - `src/components/{capture,members,dashboard,inbox,ui}/`
  - `src/repositories/` 全套（8 表 + Search）

# Next Action（原子级）

**实现事件详情页 + 事件 CRUD**（用户已选定, 2026-06-22）

第一步: 把 `src/views/EventsView.vue` 从占位升级为事件列表页（跨成员, 按时间倒序, 可点进详情）。需要新增路由 `/events/:id` 指向新 `EventDetailView.vue`。

工作内容（拆分）:
1. EventsView.vue: `medicalEvent.listRecent(N)` 列表 + 成员名反查 + 点击 row → router.push(`/events/${id}`)
2. router/index.ts: 加 `/events/:id` 路由, 懒加载 EventDetailView
3. EventDetailView.vue: 加载 event + listByEvent(attachments) + 展示全字段 + 附件缩略图 + 编辑/删除按钮
4. EventEditForm 组件（modal）: 类似 MemberForm 模式, 标量字段编辑
5. 删除: ConfirmDialog + medicalEvent.delete + 返回 /events
6. 附件预览: photo 缩略图（IndexedDB Blob → objectURL）, pdf 显示文件名链接

# Blockers

- None

# Key Decisions（影响后续实现, ≤ 3 条）

- **事件详情是独立路由 `/events/:id`**（非 modal）: 详情内容多（全字段 + 多附件 + 健康问题关联）, modal 会拥挤; URL 可分享/收藏
- **附件原件读取走 IndexedDbStorageAdapter.getFile(storage_key)**: 详情页不缓存, 每次进入重新加载（家庭数据量小, 简单）
- **Inbox 归档产生的事件默认 event_type='other', doc_type=null**: 用户可在事件详情页编辑补全 —— 这正是本次任务要解锁的能力
- **HealthProblem 关联（PRD 7.4 AI 辅助）暂不做**: 需 AI 推荐 + 手动 CRUD 两套, 工作量翻倍, 先做基础事件 CRUD, 关联留下一轮

# Relevant Files（最小集）

- `src/router/index.ts` — 加 `/events/:id` 路由
- `src/views/EventsView.vue` — 列表页（当前占位）
- `src/repositories/interfaces.ts:232-252` — MedicalEventRepository（listRecent/listByMember/getById/update/delete）
- `src/repositories/interfaces.ts:321-345` — AttachmentRepository（listByEvent）
- `src/views/MembersView.vue` — modal CRUD UI 模式参考
- `src/components/members/MemberForm.vue` — 表单组件结构参考
- `src/components/ui/{ModalOverlay,ConfirmDialog}.vue` — 复用
- `src/composables/useRepositories.ts` — Repository 入口
- `db/migrations/001_initial.sql:43-58` — medical_events schema（编辑字段约束）
